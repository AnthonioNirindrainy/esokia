{% extends 'base.html.twig' %}

{% block title %}Produit index{% endblock %}

{% block body %}
    <div class="container">
    <h1>Produit index</h1>
    <table class="table">
        <thead>
            <tr>
                <th>NomProduit</th>
                <th>Prix</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for produit in produits %}
            <tr>
                <td>{{ produit.nomProduit }}</td>
                <td>{{ produit.prix }}</td>
                <td>{{ produit.description }}</td>
                <td>
                    <a href="{{ path('app_produit_show', {'id': produit.id}) }} " class="btn btn-outline-info">Voir</a>
                    <a href="{{ path('app_produit_edit', {'id': produit.id}) }}" class="btn btn-outline-secondary">Modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">Produit vide</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <a href="{{ path('app_produit_new') }}" class="btn btn-primary btn-sm btn-block">Créer</a>
    </div>

    
{% endblock %}
